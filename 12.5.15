# 12.5.15
#вход получает две последовательности целых чисел,
#1 2 3 4 5 6 7 8
#2 4 6 8 10 12
# а возвращает список элементов,
# встречающихся только в одной из последовательностей.
# Ответ для проверки
# 1 3 5 7 10 12


a = input('Введите через пробел цифры 1-ой последовательности: ').split()
b = input('Введите через пробел цифры 2-ой последовательности: ').split()
a_set, b_set = set(a), set(b)

unique_symbol = a_set.symmetric_difference(b_set)

print(sorted(list(map(int, unique_symbol))))
